#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int N, digit, reversed=0;
    scanf("%d", &N);
    if(N==0){
        printf("Zero\n");
        return 0;
    }
    if(N<0){
        printf("Minus");
        N=-N;
    }
    int temp=N;
    while(temp>0){
        digit=temp%10;
        reversed=reversed*10+digit;
        temp=temp/10;
    }
    while(reversed>0){
        digit=reversed%10;
        switch(digit){
            case 0:printf("Zero ");
                break;
            case 1:printf("One ");
                break;
            case 2:printf("Two ");
                break;
            case 3:printf("Three ");
                break;
            case 4:printf("Four ");
                break;
            case 5:printf("Five ");
                break;
            case 6:printf("Six ");
                break;
            case 7:printf("Seven ");
                break;
            case 8:printf("Eight ");
                break;
            case 9:printf("Nine ");
                break;
                
        }
        reversed=reversed/10;
    }
    printf("\n");
    return 0;
}
