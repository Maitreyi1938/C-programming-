#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int n, i=2, Prime=1;
    scanf("%d", &n);
    if(n<=1){
        Prime=0;
    }else{
        while(i*i<=n){
            if(n%i==0){
                Prime=0;
                break;
            }
            i++;
        }
    }
    if(Prime)
        printf("Prime\n");
    else
        printf("Not Prime\n");
    return 0;
}
